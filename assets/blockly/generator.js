/*
 * Geradores de código para os blocos customizados definidos em blocks.js.
 * O gerador JavaScript já vem embutido no bundle da unpkg,
 * disponível globalmente como: javascript.javascriptGenerator
 *
 * Exemplo de registro de um bloco customizado:
 *
 * javascript.javascriptGenerator.forBlock['nome_do_bloco'] = function(block) {
 *     const code = "// código gerado aqui\n";
 *     return code;
 * };
 */

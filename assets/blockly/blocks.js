/*
 * Blocos customizados (ex: Arduino/ESP) entram aqui como:
 *
 * Blockly.Blocks['nome_do_bloco'] = {
 *     init: function() {
 *         this.appendDummyInput()
 *             .appendField("texto do bloco");
 *         this.setPreviousStatement(true, null);
 *         this.setNextStatement(true, null);
 *         this.setColour(160);
 *     }
 * };
 */
